\documentclass[answers]{exam}
\usepackage{../HT2024}

\title{Analysis II -- Sheet 6\\MVT}
\author{YOUR NAME HERE :)}
\date{Hilary Term 2024}
% Accurate as of 05/07/2024


\begin{document}
\maketitle
\begin{questions}

\question%1
Let $f(x)=\left(x^{2}-1\right)^{n}$. Prove, using Rolle's Theorem, that for $r=0,1, \ldots, n, f^{(r)}(x)$ is a polynomial whose value is 0 at no fewer than $r$ distinct points of $(-1,1)$. Hence prove that $f^{(n)}(x)$ is a polynomial of degree $n$, with distinct real roots, all of which lie in $(-1,1)$.



\question%2
\begin{parts}
\part%2a
For which real values does the polynomial $f(x):=1+x+\cdots+x^{2 m-1}$ take the value 0? What can you say about the sign of $f(x)$ as $x$ varies?

\part%2b
Prove that the function \[
	g(x):=1+x+\frac{x^{2}}{2}+\cdots+\frac{x^{n}}{n}
\] has no real roots when $n$ is even. What can you say about the roots of $g$ when $n$ is odd?
\end{parts}



\question%3
Let $f: \mathbb{R} \rightarrow \mathbb{R}$ be twice differentiable, with $f''(x) \geq 0$ for all $x \in \mathbb{R}$. Prove that \[
	f\left(\frac{x+y}{2}\right) \leq \frac{1}{2}(f(x)+f(y)) \text{ for all } x, y \in \mathbb{R}.
\]



\question%4
\begin{parts}
\part%4a
Let $f: \mathbb{R} \rightarrow \mathbb{R}$ be differentiable and let $a \in \mathbb{R}$. Suppose that $f''(a)$ exists. Prove that \[
	\lim _{h \rightarrow 0} \frac{f(a+h)+f(a-h)-2 f(a)}{h^{2}}=f''(a).
\]

\part%4b
Assume that $f: \mathbb{R} \rightarrow \mathbb{R}$ be twice differentiable on $\mathbb{R}$. Suppose that $f$ satisfies the following inequality: \[
	f\left(\frac{x+y}{2}\right) \leq \frac{1}{2}(f(x)+f(y)) \text { for all } x, y \in \mathbb{R}.
\] Using (a), show that $f''(a) \geq 0$ for all $a \in \mathbb{R}$.
\end{parts}



\question%5
Suppose that $f: \mathbb{R} \rightarrow \mathbb{R}$ is twice differentiable everywhere.
\begin{parts}
\part%5a
Prove that if $f'(x)=a f(x)$ for all $x$, then $f(x)=A e^{a x}$ for some constant $A$.

\part%5b
Prove that if $f''(x)-5 f'(x)+6 f(x)=0$ then $f(x)=A e^{2 x}+B e^{3 x}$ for some constants $A, B$. [\emph{Hint: consider $g(x)=f'(x)-2 f(x)$ and $h(x)=f'(x)-3 f(x)$.}]

\part%5c
Prove that if $f''(x)+25 f(x)=0$ then $f(x)=A \cos (5 x)+B \sin (5 x)$ for some constants $A, B$. [\emph{Hint: consider $g(x):=f(x)-A \cos (5 x)-B \sin (5 x)$ for suitable $A$, $B$; now take a hint from the applied mathematicians and consider $\frac{25}{2} g(x)^{2}+\frac{1}{2} g'(x)^{2}$.}]

\part%d
What can be said about the solutions of $f''(x)-4 f'(x)+4 f(x)=0$?
\end{parts}



\section*{Bonus Questions (optional, for the enthusiasts)}

\question%6
Let $f:[0,1] \rightarrow \mathbb{R}$ be continuous on $[0,1]$ and differentiable on $(0,1)$.
\begin{parts}
\part%6a
Suppose $f'\left(0^{+}\right):=\lim _{x \rightarrow 0^{+}} f'(x)$ exists. Show that the right-derivative $f_{+}'(0):=$ $\lim _{x \rightarrow 0^{+}} \frac{f(x)-f(0)}{x-0}$ exists and equals $f'\left(0^{+}\right)$.

\part%6b
Give an example where $f_{+}'(0)$ exists but $f'\left(0^{+}\right)$ does not.
\end{parts}



\question%7
Let $f(x)$ be a differentiable function on $[a, b]$ with $f'(x) \neq 0$ for any $x \in[a, b]$ (taking one-sided derivatives at the endpoints).
\begin{parts}
\part%7a
Show that $f(x)$ is injective.

\part%7b
Show that either $f'(x)>0$ for every $x \in[a, b]$ or $f'(x)<0$ for every $x \in[a, b]$.

\part%7c
Deduce Darboux's theorem: for any function $g$ that is differentiable on an interval, $g'$ satisfies the conclusion of the IVT without any assumption of continuity of $g'$.
\end{parts}



\question%8
(Fiendish) Suppose $f: \mathbb{R} \rightarrow \mathbb{R}$ is an infinitely differentiable function that satisfies the property that for every $x \in \mathbb{R}$ there exists an $N$ such that for all $n \geq N, f^{(n)}(x)=0$. Prove that $f$ is a polynomial.

\end{questions}

\end{document}
